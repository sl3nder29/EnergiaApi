name: CI/CD - EnergiaApi

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*.*.*'     # Exemplo: v1.0.0 -> ativa deploy de produção
  pull_request:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  CONTAINER_NAME: energiaapi
  STAGING_PORT: 8080
  PRODUCTION_PORT: 80

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: 📦 Checkout do repositório
        uses: actions/checkout@v4

      - name: ⚙️ Configurar .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: 🧩 Restaurar dependências
        run: dotnet restore

      - name: 🏗️ Build da aplicação
        run: dotnet build --configuration Release --no-restore

      - name: 🧪 Run tests (skip if no test project)
        run: |
          if ls **/*Tests.csproj 1> /dev/null 2>&1; then
            dotnet test --no-build --verbosity normal
          else
            echo "Nenhum projeto de teste encontrado. Pulando etapa de testes."
          fi

      - name: 🐳 Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Login no GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Gerar metadados da imagem
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: 🧱 Build e Push da imagem Docker (Staging)
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging
          labels: ${{ steps.meta.outputs.labels }}

  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: 🚀 Deploy automático no ambiente STAGING
        run: |
          echo "Deploy no ambiente STAGING iniciado..."
          echo "Executando container com porta ${STAGING_PORT}"
          docker run -d -p ${STAGING_PORT}:80 \
            -e ASPNETCORE_ENVIRONMENT=Staging \
            --name ${{ env.CONTAINER_NAME }}-staging \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging
          echo "✅ Deploy Staging concluído."

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: 🚀 Deploy automático no ambiente PRODUÇÃO
        run: |
          echo "Deploy no ambiente PRODUÇÃO iniciado..."
          echo "Executando container com porta ${PRODUCTION_PORT}"
          docker run -d -p ${PRODUCTION_PORT}:80 \
            -e ASPNETCORE_ENVIRONMENT=Production \
            --name ${{ env.CONTAINER_NAME }}-prod \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging
          echo "✅ Deploy Produção concluído."
